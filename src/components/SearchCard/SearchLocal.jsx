
const SearchLocal = () => {
    const localGetdata =JSON.parse(localStorage.getItem('search'));
    console.log(localGetdata)
    return (
        <div>
            
        </div>
    );
};

export default SearchLocal;